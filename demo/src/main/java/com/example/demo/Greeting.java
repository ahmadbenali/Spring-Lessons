package com.example.demo;

// Java Record class it has only one job (hold data)
// You can put inside it a validation like if the number lower then 10 etc..
public record Greeting(long id, String content){}
